[project]
name = "invoiceocr"
version = "1.0.0"
description = "发票 OCR 识别工具 - 支持图形界面的批量发票识别和统计分析"
requires-python = ">=3.8"
dependencies = [
    "openpyxl>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pyinstaller>=5.0.0",
]
